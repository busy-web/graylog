import systemInfo from 'dummy/utils/system-info';
import { module, test } from 'qunit';

module('Unit | Utility | system info');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = systemInfo();
  assert.ok(result);
});
